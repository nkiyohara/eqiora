# Three-dimensional Taylor--Green breakdown experience

Status: future-state experience contract. It is distinct from and does not
advance the analytic or manufactured `fluid.taylor-green` candidate.

## Responsibility and public claim

The visible title is **Eqiora 3D Taylor--Green numerical study**. The film
shows one exact full-cube incompressible `Re=1600` Eqiora FVM/BDF1 computation
from the fixed Taylor--Green initial state through vortex-sheet development,
small scales, and peak physical dissipation in a triply periodic domain.

The accepted result publishes four scalar histories -- dimensionless kinetic
energy `E*`, enstrophy `Z*`, physical viscous dissipation `epsilon_nu*`, and
right-endpoint energy loss `epsilon_E*` -- together with spatial and temporal
scalar-history refinement, mass/gauge/periodic/symmetry/energy-ledger
diagnostics, exact peak-state lineage, and one deterministic vorticity-
magnitude surface sequence at `|omega|*=5`.

This is an Eqiora-owned analytic/conformance/ledger/refinement numerical
study. It is not an exact post-initial solution, external numerical-history
comparison, experimental validation, published-history agreement, DNS
certification, universal benchmark truth, turbulence-model validation,
singularity study, local-field or surface-geometry convergence result,
distributed/device result, or production-HPC performance claim.

## Storyboard

| Presentation time | Content |
|---|---|
| 0--2 s | Periodic cube, exact initial velocity symmetries, `Re=1600`, Eqiora discretization, campaign envelope, and physical interval |
| 2--12 s | Fixed or slowly prescribed camera shows the deterministic `|omega|*=5` vorticity-magnitude surfaces through the exact accepted physical-dissipation peak state |
| 12--15 s | The exact panels “Eqiora histories,” “Spatial refinement,” “Temporal refinement,” and “Energy ledger and invariants” mark current time, accepted peak identity, and frozen scalar criteria |
| 15--18 s | Labelled neutral cut to the initial symmetric state; decay is never reversed |

Q-criterion and `Q*` are absent. Isovalue, scale, periodic chart, camera,
clipping, and opacity are frozen and cannot change in response to individual
frames.

## Evidence and falsifiers

Two mutually isolated derivation routes precommit every new initial
formula, cell average, integral, normalization, exact identity, discrete
ledger term, acceptance criterion, expected value, tolerance, solver
criterion, and arithmetic floor before Eqiora output is visible. One agent may
author both routes when neither reads the implementation path or the other's
output. Eqiora histories are producer results, not post-hoc expected values.

The reusable 3D periodic collocated class first proves one ordinary positive
path, then its exact seam, checkerboard, retained-flux, JVP, ledger, resource,
and hidden-parity falsifiers. The full Taylor--Green campaign must then pass
mass and pressure-gauge checks, periodic replay, observed symmetry plus an
off-class hidden-symmetry probe, physical/numerical energy separation,
discrete-ledger closure, exact peak retention, complete lineage, and frozen
spatial and temporal scalar-history criteria.

Self-refinement is evidence only for that declared scalar consistency and
convergence boundary. It does not establish continuum truth after breakdown,
independent post-initial trajectory truth, local-field convergence, or
surface-geometry convergence. A plausible history, matching peak, or striking
surface cannot override any failed ordinary-path, class, balance, ledger,
symmetry, refinement, lineage, resource, or full-campaign gate.

No UCL, Southampton/OpenSBLI, Fourier, or other external or independent
post-initial numerical history, source peak, source band, comparison curve,
or comparison statement participates in acceptance or publication.

## Capability and artifact dependencies

- the honest #149 dependency boundary and accepted executable 3D periodic
  semantic reference capability;
- an accepted reusable collocated 3D periodic class with independent
  conformance evidence;
- the exact full-cube incompressible Eqiora FVM/BDF1 ordinary path and complete
  precommitted campaign;
- immutable Model, Realization, source, environment, state, Result, history,
  peak, projection-input, and scene lineage;
- deterministic periodic vorticity-magnitude surface extraction and bounded
  common 3D presentation; and
- common publication, accessibility, resource, and dossier admission.

The film does not justify a general volume viewer, turbulence path, arbitrary
periodic topology, Reynolds family, durable large-result schema, or unbounded
result scale.

## Accessibility and promotion

The reduced-motion still shows the exact accepted peak-dissipation state, a
section or silhouette readable without depth motion, and the full Eqiora
energy history. Its text alternative describes the development from the
initial symmetric state and identifies the surface as a deterministic
presentation projection, not a quantitative accuracy oracle.

Every application and publication surface carries this visible disclosure:

> Eqiora-computed histories and surfaces are accepted by analytic initial
> checks, reusable-class conformance, balance and energy-ledger checks,
> symmetry checks, and precommitted spatial/temporal scalar-history
> refinement. They are not an external-history comparison, DNS certification,
> or claim of post-breakdown continuum truth or local surface convergence.

Promotion requires the accepted 3D periodic reference and reusable class,
dual derivation and review, the complete precommitted Eqiora campaign,
immutable result admission, deterministic vorticity projection, and common
publication admission. The dossier marks community numerical comparison and
experimental validation as absent/not claimed rather than inferring them from
self-refinement.

## Primary source

M. E. Brachet et al.,
[“Small-scale structure of the Taylor--Green vortex”](https://doi.org/10.1017/S0022112083001159),
*Journal of Fluid Mechanics* 130, 1983, supplies historical problem context
only. No published numerical history, ordinate, peak, band, or comparison
value is extracted from it for this first-gallery claim.

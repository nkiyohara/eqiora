---
title: Composition
description: Components, explicit bindings and conserving connections in the current Eqiora source.
editUrl: https://github.com/nkiyohara/eqiora/edit/main/docs/site/src/content/docs/reference/language/composition.mdx
---

import { Code } from '@astrojs/starlight/components';
import ExactSourceLink from '@components/site/ExactSourceLink.astro';
import source from './_examples/composition.eqi?raw';

[Language overview](/reference/language/) · [Install this source revision](/get-started/#install-with-uv)

A `component` is a reusable declaration. An `instance` creates one occurrence with
explicit named bindings. `public` requirements live inside the component body.

<Code code={source} lang="eqiora" title="composition.eqi" />

`input` is required; `offset` has a default. The two occurrences own separate
private `output` Fields and relations. Equal input values would not merge those
occurrences. Missing required parameters and unknown bindings are errors.

## Ports and physical connections

[Electrical components](/reference/standard-packages/electrical/) expose named
`positive` and `negative` conserving ports. `connect conserving` connects their
typed connector occurrences: across values agree and outward through values sum
to zero. It does not paste one component's equations into another or impose an
execution order. A signal input/output port has different semantics.

## Binding existing spatial Fields

A continuum component can require `support body: volume(ambient_dimension = 2)`
and an occurrence-bound `variable` or `state` in its signature. In an instance, write
`support body = body` and `field displacement = displacement` explicitly.
The [elastic-body example](/reference/standard-packages/continuum/#elastic-balance-and-boundaries)
binds the same displacement to a balance law and its boundary interface. These
bindings refer to one existing Field, not two copied displacement states.

Exact support and connector identity matter; equal numerical sizes are insufficient.
Complete boundary families must include the actual exterior members.

## Source files and packages

A package-qualified declaration additionally requires its exact dependency closure.
See [source files and installation](/reference/standard-packages/#source-and-installation-boundary)
for the source-installed workflow.

<ExactSourceLink kind="blob" path="docs/site/src/content/docs/reference/language/_examples/composition.eqi">Example source</ExactSourceLink>
· <ExactSourceLink kind="tree" path="crates/eqiora-compiler/src/hierarchy">hierarchy compiler</ExactSourceLink>
· Next: [Standard sources](/reference/standard-packages/)

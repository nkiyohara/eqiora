model Counter {
  clock tick = periodic(period = 1 / 10, phase = 0 / 1);
  state memory: 1 at tick;
  initial { memory = 0; }
  relation advance at tick {
    next(memory) - pre(memory) - 1 = 0;
  }
}

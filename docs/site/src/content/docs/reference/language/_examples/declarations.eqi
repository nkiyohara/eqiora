model Current {
  parameter voltage: kg * m ^ 2 / (s ^ 3 * A) = 12;
  parameter resistance: kg * m ^ 2 / (s ^ 3 * A ^ 2) = 4;
  variable current: A;
  relation ohm {
    voltage - resistance * current = 0;
  }
}

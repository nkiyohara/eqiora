---
title: Differentiation and framework adapters
description: The bounded differentiable program and its PyTorch and JAX projections.
---

import ExactSourceLink from '@components/site/ExactSourceLink.astro';

Eqiora differentiates an accepted implicit relation through primal, JVP, and VJP contracts. Python compiles an opaque `DifferentiableProgram` from one exact common scalar `Plan`, ordered Parameter references from that Plan's Model, and the Plan's exact output Field. The verified Python slice is a caller-supplied two-dimensional rectangular Cartesian Mesh resolved with Q1 FEM or cell-centred TPFA and a typed linear solve policy.

The first alpha verifies one host-CPU rank-one `float64` scalar-elliptic path. The optional PyTorch adapter uses the accepted VJP in first-order backward; the optional JAX adapter uses typed native CPU FFI for primal, JVP, and VJP. Base `import eqiora` imports neither framework, and no device transfer is hidden.

Read the maintained <ExactSourceLink path="docs/python/differentiation.md" kind="blob">differentiation contract</ExactSourceLink> for exact versions, examples, transformations, and explicit nonclaims.

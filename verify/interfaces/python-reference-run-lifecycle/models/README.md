# Models

The executable target embeds a scalar decay model and an overdetermined scalar
model. Keeping the exact source beside the Python calls makes lifecycle races
independent of fixture-path or packaging behavior.

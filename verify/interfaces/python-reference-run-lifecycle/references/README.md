# Reference

The oracle is the public Rust `ModelDocument::run_reference_plan_controlled`
path. Its observer is invoked only at fully accepted semantic-execution
boundaries and returns either a complete trajectory or typed cancellation
evidence without a partial trajectory.

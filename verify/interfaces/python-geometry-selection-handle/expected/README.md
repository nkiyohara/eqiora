# Expected behavior

The Geometry owner resolves a canonical name into an immutable handle, and the
existing Mesh correspondence returns the same membership as its string
compatibility route. Assertions are relational; this case adds no expected
numeric value, tolerance, canonical-byte baseline, or scientific oracle.

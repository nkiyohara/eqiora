# Expected observations

The clean installed-wheel Marimo host exposes the accepted Geometry and Mesh
carrier names, independently derived counts, private scene marker, shared
controls, exact Mesh-owned selection, and read-only canvas without external
runtime traffic.

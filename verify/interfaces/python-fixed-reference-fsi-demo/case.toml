id = "interfaces.python-fixed-reference-fsi-demo"
status = "verified"
scope = "one-installed-python-immutable-fixed-reference-monolithic-fsi-two-step-result"
reference_kind = "shared-rust-plan-common-result-typed-evidence-lineage"

capabilities = [
  "python-fixed-mesh-monolithic-fsi-plan-run",
  "python-common-fsi-result-and-typed-evidence",
  "python-coupled-model-realization-run-state-trajectory-lineage",
  "python-read-only-coupled-interface-state-arrays",
]

[problem]
model = "one packaged byte-exact copy of verify/fsi/fixed-reference-monolithic-step-2d/models/direct.eqi, compiled explicitly by Python through the current Model owner"
physics = "verified fixed-reference inertial-fluid linear-solid monolithic backward-Euler coupling"
realization = "the existing fieldwise coupled multi-Domain plan over the exact 9-vertex 8-affine-triangle two-body mesh"
accepted_steps = 2
intent = "immutable keyword-only eqiora.fsi.FixedMeshMonolithic with every initial-state, time, scale, solver-tolerance, and iteration input explicit"
resolution = "eqiora.fsi.resolve(model, intent) returns one exact-Model-bound FixedMeshMonolithicPlan before execution"
entry_point = "eqiora.submit(model, plan=plan).result(), with eqiora.run(model, plan=plan) as the synchronous common-Result route"
result_type = "immutable common eqiora.Result whose trajectory property returns the accepted Trajectory"
evidence = "eqiora.fsi.fixed_mesh_monolithic_evidence(result), with exact State-keyed state evidence"
presentation_successor = "interfaces.python-trajectory-field-stills owns common scalar and deformed stills over this common Result's accepted Trajectory"

[oracle]
scientific_case = "fsi.fixed-reference-monolithic-step-2d"
trajectory_case = "artifacts.fixed-reference-fsi-spatial-trajectory"
lineage_case = "interfaces.studio-fixed-reference-fsi-demo"
equivalence_case = "interfaces.structural-semantic-fingerprint"
model_admission = "structural semantic equivalence to the accepted current source, preserving the caller's current artifact identity; digest equality is neither required nor claimed"
production_comparison = "the existing Rust scientific tests compare the production composition against the independent support composition for solver coefficients and for ordered two-state, state, trajectory, and Run lineage"
thresholds = "restated only by reference from the registered scientific cases; never re-derived, tuned, or relaxed here"
arrays = "coindexed partition, velocity, bubble, pressure-support, displacement, and interface-action arrays in coherent SI order"

[acceptance]
packaged_source_is_byte_exact = true
caller_compiles_and_supplies_current_model = true
intent_has_no_hidden_defaults = true
plan_is_bound_to_the_exact_model_revision = true
unsupported_intent_rejects_before_worker_creation = true
studio_and_python_consume_one_production_rust_composition = true
admission_is_structural_and_never_digest_equality = true
caller_current_artifact_identity_is_retained_in_lineage = true
structural_semantic_equivalence_is_the_claimed_criterion = true
equal_exact_model_digest_is_not_claimed = true
production_and_support_compositions_agree_in_existing_rust_tests = true
no_scientific_expected_value_tolerance_or_falsifier_changes = true
exact_partition_is_nine_vertices_and_eight_triangles = true
two_ordered_accepted_steps_are_exposed = true
run_result_is_the_common_result = true
result_trajectory_is_memoized = true
fsi_evidence_is_selected_by_exact_trajectory_state = true
mini_bubble_velocity_block_is_exposed = true
fluid_p1_pressure_support_is_exposed = true
solid_displacement_is_exposed = true
free_interface_action_is_exposed = true
native_energy_and_acceptance_scalars_are_exposed = true
solver_and_assembly_evidence_are_exposed = true
model_realization_run_state_trajectory_lineage_is_relationally_exact = true
arrays_are_memoized_read_only_and_owner_independent = true
independent_solves_do_not_share_array_storage = true
numpy_import_is_lazy_until_array_materialization = true
step_selection_does_not_change_solver_values = true

[falsifiers]
foreign_model_meaning = "rejected before Realization"
digest_equality_used_as_admission = "admission that compares current Model digests instead of the closed structural projection is rejected"
foreign_solver_tuple = "rejected by the shared Rust application boundary"
model_realization_run_or_trajectory_drift = "public canonical lineage fields disagree"
foreign_or_stale_lineage_digest = "rejected before result construction"
duplicate_result_authority = "a second private FSI result or coupling semantics outside the production composition is rejected"
support_composition_as_production_authority = "the independent support composition is comparison evidence only and cannot serve the demo"
partition_shape_or_connectivity_drift = "projection rejects noncanonical, reordered, or out-of-range partition data"
missing_mini_bubble_block = "velocity projection rejects a bubble-free block inventory"
pressure_support_outside_fluid_closure = "pressure projection fails closed"
nonzero_displacement_outside_solid_closure = "displacement projection fails closed"
wrong_free_interface_action_cardinality = "interface-action projection fails closed"
duplicated_reordered_or_nonconsecutive_accepted_step = "ordered step and coordinate inventory disagree"
step_selection_out_of_range = "IndexError before any projection"
nonfinite_or_wrong_unit_value = "array or scalar unit check fails"
action_and_energy_unit_conflation = "exposed scalars disagree with their declared units"
solver_stopping_presented_as_physics_acceptance = "acceptance scalars and stopping report are distinguished"
reauthored_or_relaxed_acceptance_threshold = "referenced scientific thresholds differ from their owning cases"
mutable_or_result_owned_arrays = "array lifetime and writeability checks fail"
eager_numpy_dependency = "isolated process imports NumPy before first array projection"
surviving_predecessor_python_api = "FixedReferenceFsiStep, FixedReferenceFsiResult, or solve_fixed_reference_fsi remains reachable from the native module, Python facade, exports, shipped stub, generated API docs, or installed wheel"
ordinal_only_evidence_lookup = "state evidence accepts an ordinal or a State from another occurrence instead of the exact accepted State"

[claim_boundary]
one_current_model = true
historical_generation_callable = false
one_coupled_realization = "existing fieldwise coupled multi-Domain plan"
intrinsic_dimension = 2
fixed_reference_configuration_only = true
two_consecutive_accepted_steps_only = true
monolithic_single_step_coupling_only = true
general_fsi = false
same_time_iterative_or_partitioned_coupling = false
generic_coupling_graph = false
ale_moving_mesh_or_remeshing = false
advection_or_navier_stokes = false
other_model_geometry_mesh_or_time_plan = false
three_dimensional = false
python_callbacks_or_manual_time_loop = false
application_recomputes_physics = false
stress_drag_or_lift = false
validation_or_benchmark = false
video_or_animation = false
result_trajectory_generality_beyond_the_accepted_case = false
interactive_viewer = false

[precommitment]
precommitted = true
authored_before_implementation_by_a_nonimplementing_agent = true
oracle_owner = "this manifest and the referenced case READMEs; an implementer returns a proof rather than editing them"
base_revision = "00a5ec02d5dcbd7760929ce1fcbb831e9eda2d89"

[evidence]
runner = "python-installed-wheel"
script = "tools/ci/python_distribution_gate.py"

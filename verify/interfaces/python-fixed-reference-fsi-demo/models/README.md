# Input Model

The installed wheel carries the equations-only `FixedReferenceFsi2d`
Component. Python supplies the exact adjacent-partition Geometry and explicit
physical parameters to ordinary compilation, then uses root common resolution
and execution. No historical codec selector, application-shaped Intent, second
FSI Model fixture, or Python-authored coupling graph is introduced.

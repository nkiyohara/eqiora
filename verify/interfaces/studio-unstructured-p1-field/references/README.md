# Reference strategy

The accepted `FieldSnapshotEnvelopeV1`, its single scalar vertex block, and the
validated fixed-spatial artifact context are the independent reference inputs.
The application projection must reproduce their identities, canonical mesh
coordinates/connectivity, values, and exact finite extrema.

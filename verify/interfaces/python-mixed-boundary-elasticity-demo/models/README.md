# Input Model

The installed wheel carries a build-copied byte-exact copy of
`verify/solid/mixed-boundary-elasticity-2d/models/direct.eqi`. Python reads and
compiles that source explicitly with exact codec v4 before calling the bounded
structural operation. No second scientific Model fixture is introduced here.

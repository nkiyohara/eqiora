# Input Model

The installed wheel carries a build-copied byte-exact copy of
`verify/solid/mixed-boundary-elasticity-2d/models/direct.eqi`. Python reads and
compiles that source through the ordinary current owner before calling the
bounded structural operation. No historical codec selector or second
scientific Model fixture is introduced here.

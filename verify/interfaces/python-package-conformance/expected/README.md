# Frozen structural conformance facts

`identities.json` was derived before the conformance adapter existed. The
primary route constructed a renamed release from the already compiler-derived
private `Main` declaration and source of the accepted Poisson package, replaced
only its documentation with a deliberately false scientific statement, and
then used the protected predecessor's `PackageReleaseV1`,
`ResolutionRecordV1`, and `PackagedModelDocument::compile_locked` contracts.
The secondary route reads the already accepted `org.example.poisson` release,
resolution, compilation record, and current artifact facts in place.

Neutral key names keep these expected facts outside the current-identity
transition vocabulary. No test regenerates this file through the operation
under test. The facts freeze structural identity only; they are not a report
wire, attestation, publisher statement, numerical oracle, or trust decision.

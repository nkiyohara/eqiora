# Installed Python structural check for one exact Model Package

This case freezes the installed-Python
`check_package_conformance(..., profile="eqiora.package.structural-conformance-v1")`
boundary. One explicit external store, exact canonical resolution bytes, and
one bare root-local selector pass through the unchanged locked compiler twice.
The immutable in-process report names package, toolchain, compilation, and
canonical Model identity facts only after exact record and artifact replay
agree.

The primary fixture has a private `Main` and deliberately false scientific
documentation. Its structural acceptance proves that the report is not a
physics, numerical, support, maturity, evidence, publisher, or trust verdict.
The secondary accepted `org.example.poisson` closure keeps its independent
registered evidence unchanged; the report neither discovers nor reproduces
that evidence.

## Falsifiers

The installed and native oracles freeze the sole profile token, public/stub
signature, exact `NamedTuple` field order, immutability, direct Rust parity,
two-compilation agreement, record and current artifact replay, complete
diagnostic ordering, canonical resolution bytes, typed release normalization,
source and dependency-alias changes, exact filesystem authority, selector
admission, and accepted-only return. Successful and rejected calls leave the
selected store byte-for-byte unchanged.

Resource overflow and invalid unused definitions remain owned by
`packages.package-definition-validation`; exact offline store behavior remains
owned by `packages.offline-model-package`. This case invokes those explicit
registered cases during integration instead of allocating another extreme
mutant.

## Boundary

There is no discovery, installation, store mutation, registry, network,
version solving, package code or test execution, callback, plugin, report wire,
schema, digest, signing, badge, attestation, trust service, automatic evidence
lookup, execution, scientific correctness, supported-physics decision, or
Studio surface. Runtime values are caller-constructible Python tuples and are
never consumed as authority.

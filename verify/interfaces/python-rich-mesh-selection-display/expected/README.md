# Expected evidence

The registered installed-candidate gate owns the exact Python, frontend,
JupyterLab, marimo, browser, package, and wheel-local asset observations in
`case.toml`. The entity counts and closures are reused from the accepted
Geometry↔Mesh correspondence; this case introduces no scientific value,
tolerance, screenshot, or image baseline.

# Model inputs

This adapter case reuses the packaged current Model consumed by
[`interfaces.python-fixed-reference-fsi-demo`](../../python-fixed-reference-fsi-demo/README.md).
It owns no additional Model fixture.

# References

The scalar numerical oracle is backward Euler for `dx/dt + x = 0`, yielding
`x_n = (1 + dt)^(-n)`. Exact artifact replay and physical node inventory are
checked through the public Rust facade. Shared Rust draft, compiler, and
Semantic Kernel contracts are the only authorities for identity, dimension,
Domain membership, and closed-draft admission.
Route comparison additionally uses the single shared contract specified by
[RFC 0073](../../../../rfcs/0073-structural-semantic-fingerprint.md); exact
artifact replay remains a separate identity claim.
The spatial numerical check reuses the existing scalar-elliptic Cartesian
lowerer and serial FEM application operation; it is not a Python solver API or
a second numerical implementation.

# Model input

The registered PyO3 test reuses the accepted scalar-Poisson source owned by
`interfaces.python-native-modeling` and also constructs one small temporal
counterexample in memory. This case adds no mathematical fixture, equation,
coefficient, expected value, or tolerance.

# Expected API observations

The ordinary path accepts a typed model-unbound Cartesian density request,
publishes the exact effective Mesh realized from the Model Domain, binds both
only in Plan, and publishes distinct Realization identities for Q1
and TPFA, and retains the closed
effective policy/provider/placement projections named in `case.toml`.
Execution retains the Plan identity and method-native vertex versus cell-centre
Field association. Negative probes publish no Plan. No scientific values or
tolerances are introduced here.

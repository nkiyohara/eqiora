# Expected behavior

The installed source plus Python Geometry yields one common Model first.
Missing, foreign/stale, kind-swapped, raw-name, extra, and non-finite inputs
then reject before a Model is returned.

# Inputs

The complete committed source tree is the candidate input. The gate does not
introduce a second package fixture: it builds the root source distribution and
requires every wheel to originate from its extracted contents.

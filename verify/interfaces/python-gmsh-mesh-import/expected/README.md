# Expected behavior

Installed Python returns the existing immutable `Mesh`, whose external-import
manifest relates the caller's complete source digest and normalized arrays to
that Mesh. The same exact accepted import reaches the existing steady-Stokes
Plan boundary. Assertions are relational; this case adds no expected numeric
value, tolerance, canonical-byte baseline, or scientific oracle.

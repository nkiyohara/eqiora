# Model source

The evidence script reads the byte-exact packaged component-only
`fixed-reference-fsi.eqi` resource from the installed wheel and authors its
adjacent Geometry and common Mesh in Python. This directory intentionally owns
no copied or re-authored model.

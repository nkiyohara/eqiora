# Expected application observations

No scientific scalar, tolerance, digest literal, Figure bytes, or pixel
baseline is added here.

The live Marimo page must expose the eight public carrier type names used by
the composition. Run and Result must display the same runtime-derived
lowercase SHA-256 identity. The typed evidence summary must name pressure in
Pa, cylinder force on the fluid in N/m, and net flux in m²/s without comparing
their values. The caller-owned pressure Figure must decode to positive natural
dimensions; its dimensions and pixels remain unfrozen.

The exact readiness text is `EQIORA_EXACT_CYLINDER_STOKES_READY`. It counts
only after the typed summary and Figure are present.

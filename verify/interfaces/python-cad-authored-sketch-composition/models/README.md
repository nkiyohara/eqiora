# Models

No source or durable Model fixture is authoritative for this case. The
installed-Python oracle constructs the two closed sketch inputs through the
frozen public API and compares their graph and Geometry products with accepted
predecessor authorities. This metadata directory adds no Model, sketch wire,
profile, operation graph, or provider-local fixture.

# Model

The executable case embeds one bounded scalar decay source directly beside
the PyO3/Rust oracle assertions. No canonical IDs or digests are frozen in a
fixture because each independent compilation intentionally mints fresh Model
identity.

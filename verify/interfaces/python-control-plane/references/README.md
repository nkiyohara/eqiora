# References

The normative references are Eqiora's public current-only `ModelDocument`, its
current value-edit transaction path, and the stable diagnostic-code registry.
The evidence intentionally uses no second Python semantics implementation, no
historical codec selector, and no external transport or execution protocol.

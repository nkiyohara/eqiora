# References

The normative references are Eqiora's public `ModelDocument`,
`ExactModelCodec::V6`, versioned value-edit transaction path, and stable
diagnostic-code registry. The evidence intentionally uses no second Python
semantics implementation and no external transport or execution protocol.

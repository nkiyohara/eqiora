# Expected evidence

The primary expected Model and compilation artifacts remain owned by
`verify/artifacts/current-model-relational-identity-transition/expected/deterministic/offline-model-package/`.
The frozen digest values are also indexed by
`verify/packages/offline-model-package/expected/identities.json`.

No expected artifact is regenerated through the Python API under test.

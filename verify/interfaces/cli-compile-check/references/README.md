# References

The semantic reference is the accepted transport-neutral operation
`eqiora::api::ModelDocument::compile(&str, &str)`. The oracle calls that operation
independently and compares the canonical identity of repeated source compilation.
A changed Parameter value must change the exact artifact digest.

The exact command, stream, resource, containment, architecture, and
installed-candidate rules are frozen by the accepted cumulative CLI
compile-check contract bundle outside the repository. The selected/decoy
fingerprints are derived at test time through the semantic reference, and
actual-main cardinality is read only from per-child ephemeral append counters
after process termination. This
directory deliberately carries no copied implementation, persisted digest,
filesystem provider, platform standard, control request, MCP request, or
published package specimen.

# References

The semantic reference is the accepted transport-neutral operation
`eqiora::api::ModelDocument::compile(&str, &str)` at protected predecessor
`280ccfe3046d7b7279f821d68f19f38b7b5a9fcd`. The oracle calls that operation
independently; it does not copy compiler logic or compare identities from two
fresh Model occurrences.

The exact command, stream, resource, containment, architecture, and
installed-candidate rules are frozen by the cumulative Issue #80 contract
bundle outside the repository. The selected/decoy fingerprints are derived at
test time through the semantic reference, and actual-main cardinality is read
only from per-child ephemeral append counters after process termination. This
directory deliberately carries no copied implementation, persisted digest,
filesystem provider, platform standard, control request, MCP request, or
published package specimen.

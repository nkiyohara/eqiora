# Expected Jupyter observations

No scientific scalar, tolerance, digest literal, Figure bytes, or pixel
baseline is added here.

The live Jupyter notebook must expose the eight public carrier type names used
by the composition. Run and Result display the same runtime-derived lowercase
SHA-256 identity. The typed evidence summary names pressure in Pa, cylinder
force on the fluid in N/m, and net flux in m²/s without comparing their values.
Exactly one caller-owned pressure Figure must decode to positive natural
dimensions, and no cell may emit stderr.

The exact readiness text is
`EQIORA_EXACT_CYLINDER_STOKES_JUPYTER_READY`. It counts only after the typed
summary and Figure have been displayed.

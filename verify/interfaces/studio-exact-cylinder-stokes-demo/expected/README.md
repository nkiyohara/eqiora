# Expected outcome

The immutable example produces one accepted exact-source/affine-realization
lineage, one SparseLU Stokes result satisfying its frozen target, named
cylinder reaction and balance evidence, and one coherent-SI pressure P1
workspace carrying the exact Model, Realization, Run, snapshot, mesh, Field,
and Domain identity.

Foreign lineage, an unregistered snapshot, excessive geometry error, the wrong
field unit, inconsistent flux or momentum arithmetic, a true residual above
target, or a foreign/malformed field stream prevents ready-state publication.
Browser preview reports that native execution is required and publishes no
scientific substitute.

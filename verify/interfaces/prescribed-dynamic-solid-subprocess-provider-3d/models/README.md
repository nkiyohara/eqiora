# Accepted source reuse

The case deliberately adds no source program.  It consumes the exact direct
prescribed-solid source and immutable resources already frozen by
`artifacts.prescribed-dynamic-solid-state-run-3d`.  Keeping one source owner
prevents a transport fixture from silently becoming a second semantics or
numerics authority.

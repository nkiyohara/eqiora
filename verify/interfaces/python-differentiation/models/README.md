# Models

The executable evidence reuses the canonical two-dimensional Poisson fixture
from `differentiation.spatial-poisson-fem-fvm`. Python compiles the same source
through the current public authoring profile; this interface case adds no
second model fixture or semantics.

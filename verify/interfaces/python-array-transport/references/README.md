# References

The Rust public facade provides the owned `ReferenceSeries` buffers. NumPy
2.1 or later provides the Python ndarray owner and versioned DLPack capsule
implementation. No external numerical reference data is required.

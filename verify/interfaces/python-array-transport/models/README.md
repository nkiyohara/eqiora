# Models

The executable evidence compiles a scalar exponential-decay model in memory.
No checked-in model fixture is required because array ownership, rather than
model syntax, is the falsified boundary.

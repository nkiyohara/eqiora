# Input Model

The sole Model input is the exact checked-in direct source at
`verify/solid/mixed-boundary-elasticity-2d/models/direct.eqi`, compiled through
the current `ModelDocument` owner. The snapshot retains that exact Model
artifact and revision, its displacement Field ULID, and the exact body support
Domain ULID. A separately compiled foreign equal-bounds Model is not the same
snapshot lineage.

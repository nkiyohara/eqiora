# Model input

This case reuses the exact canonical Model, source and target Realizations,
and V2-to-V3 spatial trajectory assembled by
`fsi.remeshing-transfer-2d`. Dataset roles and windows are a derived
interpretation and add no second Model semantics.

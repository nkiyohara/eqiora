# Expected evidence

The integration test checks canonical Dataset bytes, exact trajectory and
snapshot lineage, training-only statistics, active-support entity order,
ragged mesh-local block widths, normalized owned values, and fresh replay.
The assertions are the executable oracle; this directory stores no generated
binary output.

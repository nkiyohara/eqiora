# Model inputs

`../fixtures/unit-square.xdmf` is the bounded external metadata document and
`../fixtures/unit-square.h5` is its complete external-array source. This case
does not use an Eqiora language model: it verifies the public data-import path
that reconstructs typed mesh and field artifacts from those inputs.

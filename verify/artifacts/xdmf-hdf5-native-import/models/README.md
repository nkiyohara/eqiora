# Model inputs

The model input is the checked-in `fixtures/unit-square.xdmf` metadata document
plus the complete caller-owned `fixtures/unit-square.h5` file image. No path in
the metadata grants filesystem authority.

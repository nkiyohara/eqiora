# Expected evidence

The public-facade integration test accepts the Vertex scalar and Cell vector,
preserves their identity across provenance-only changes, and rejects crossed
references plus the selected invariant and decoder-bound violations.

Successful manifest reference validation is an assertion check, not evidence
that an external-format adapter derived the accepted artifacts.

# Expected evidence

Expected outcomes and diagnostics are literals in `case.toml` and the Rust
oracle. The accepted 17-by-17 canonical JSON and domain-separated digest are
embedded in the test so a current serializer cannot regenerate its own oracle.

# References

The independently derived axis-state products and every literal boundary value
are committed in `case.toml`. The executable oracle does not recompute its own
expected entity or closure-reference counts.

# Reference derivation

`derive_prescribed_dynamic_solid_state_run_3d.py` uses only the Python
standard library. It validates the frozen current canonical Model, derives the
body, Field, and boundary roles structurally, reconstructs all persisted
coefficient values from explicit binary64 bit patterns, renders the closed
artifact grammars, and derives every content identity and lineage edge.

Run it without arguments to verify the committed fixtures. `--write` is only
for the initial independent-oracle materialization; later writers must not use
it to tune evidence to production.

The exact accepted producer sequence and the mathematical `0.0075` scientific
oracle remain deliberately separate. The derivation asserts their agreement
within the unchanged tolerance but uses only the producer bit sequence for
persisted bytes.

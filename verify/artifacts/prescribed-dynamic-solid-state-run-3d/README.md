# Exact prescribed dynamic-solid State and Run

This case freezes one exact serial-host publication of the accepted
prescribed dynamic-solid reference step. The ordinary Rust application path
must retain the caller's current Model, replay the accepted numerical result,
and publish the standalone Realization, four logical Field observations, the
prior and accepted-next States, and a Run whose only output is the
accepted-next State.

The standard-library reference derivation reconstructs every numerical leaf
from explicit binary64 bits. In particular, persisted values replay the live
accepted producer and are not recomputed from the mathematical affine
formula. The unchanged scientific comparison and tolerance remain in
`solid.prescribed-dynamic-solid-step-3d`.

The Rust oracle checks canonical bytes, identities, lineage, resource-bound
validation, complete-object displacement mutants, current-Model semantic
replay, owner-level role substitution, and failure-atomic execution. This is
only the fixed serial-host occurrence; it adds no general structural result,
trajectory, restart, provider protocol, Python, Studio, parallel, or scale
claim.

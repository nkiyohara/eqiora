# Model input

This artifact case reuses the exact canonical Model, source and target
Realizations, remeshing association, common refinement, transfer receipt, and
V2/V3 spatial trajectory assembled by `fsi.remeshing-transfer-2d`. It adds no
format-owned Model or mesh identity.


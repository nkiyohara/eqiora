# Model

The test compiles the same canonical one-dimensional Poisson source used by
`numerics.poisson-fem-fvm`. The frozen Realization golden retains its original
historical Model v1 reference as an opaque identity; no product decoder admits
those Model bytes. New executions use the separate current Model owner.

# Model

The test compiles the same canonical one-dimensional Poisson source used by
`numerics.poisson-fem-fvm`. The resulting `ModelEnvelopeV1` is the authority
for model identity, source revision, and semantic content digest.

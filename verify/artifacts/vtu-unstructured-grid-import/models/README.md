# Model input

This adapter case has no Eqiora source-model fixture. The public integration
test imports the checked-in external mesh and selected point/cell Fields into
the existing invariant-checking mesh, Field, artifact, and provenance
contracts. VTK XML is not model meaning.

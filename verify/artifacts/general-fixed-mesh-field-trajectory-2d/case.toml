id = "artifacts.general-fixed-mesh-field-trajectory-2d"
status = "verified"
scope = "complete-durable-fixed-mesh-2d-field-trajectory-replay"
reference_kind = "independent-structural-content-dag-replay"

capabilities = [
  "general-fixed-mesh-2d-field-trajectory-boundary",
  "complete-content-addressed-dependency-replay",
  "exact-run-output-inventory",
  "order-independent-complete-artifact-catalogs",
]

[problem]
ambient_dimension = 2
source_case = "artifacts.fixed-reference-fsi-spatial-trajectory"
accepted_states = 2
field_inventory = "four Realization-selected coherent-SI physical Fields"
mesh_family = "fixed affine Tri3"
trajectory_family = "fixed-step SpatialTrajectoryEnvelopeV1"

[artifact_contract]
durable_identity = "existing Model, mesh, Field, snapshot, state, segment, trajectory, Realization, and Run artifacts"
acceptance_owner = "borrowed non-serializable complete replay proof"
catalog_policy = "declaration order independent, unique, complete, and without unused objects"
run_output = "exactly one output equal to the replayed final trajectory root"
time_series = "at least two accepted states on the exact Realization fixed-step coordinates"

[falsifiers]
non_2d_mesh = "reject"
missing_segment_state_snapshot_or_block = "reject"
duplicate_or_unused_catalog_object = "reject"
substituted_physical_field_or_block_meaning = "reject"
mutated_immutable_root_prefix = "reject"
missing_or_additional_run_output = "reject"

[claim_boundary]
general_fixed_mesh_2d_replay = true
complete_exact_artifact_dag = true
exact_run_output_inventory = true
new_durable_schema = false
variable_step = false
ale_or_remeshing = false
general_3d = false
renderer_or_media = false
restart_or_checkpoint = false
scientific_rederivation = false

[evidence]
package = "eqiora"
test = "general_fixed_mesh_field_trajectory_2d"

# Expected acceptance outcomes

The positive fixture retains two exact fixed-step states, four complete
Realization-selected Fields per state, the MINI velocity Vertex and Cell
blocks, two immutable segments, the generation-one final root, and one exact
Run output. Expected digests are derived from the already verified producer;
this case intentionally pins no newly authored hexadecimal identity.

The registered test rejects non-2D admission, missing dependency layers,
duplicate and unused catalog objects, substituted physical or block meaning,
mutated prefix identity, and both missing and additional Run outputs.

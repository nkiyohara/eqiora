# Model provenance

The executable evidence compiles the repository package
`packages/org.example.poisson` directly. No copied model is kept in this case,
so package-path drift cannot be hidden by a second source fixture.

# Reference provenance

The analytic element values, tolerances, deterministic finite-difference
direction, mutant ownership, and convergence and conservation limits are
precommitted in RFC 0075. The implementation lane only wires those values into
the focused element and package-path evidence; it does not derive or tune them.

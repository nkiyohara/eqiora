# Expected evidence

The frozen numeric expectations are asserted directly by the Rust evidence:
the focused element test covers the non-square local oracle, while
`compiled_package_poisson_form` covers the ordinary package and Realization
path. No generated golden table is required for this case.

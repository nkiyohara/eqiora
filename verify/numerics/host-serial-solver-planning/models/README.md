# Problem fixture

This case has no semantic Model input. Its complete algebraic problem is the
captured canonical CSR fixture in `../expected/policy-v1.json`, independently
derived by `../references/derive_policy_v1.py`. This directory exists to make
that absence explicit without creating a second fixture authority.

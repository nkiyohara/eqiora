# Model provenance

The executable evidence compiles
`verify/solid/isotropic-elasticity-2d/models/linear-load.eqi` directly. No copy
of the accepted Model is kept in this case, so identity or source drift cannot
be hidden by a second fixture.

# Reference provenance

The independent acceptance action is the captured complete CSR row fold in
global order. The manufactured solution supplies the end-to-end numerical
oracle; fault cases add timeout-bounded collective-liveness falsification to
the registered quantitative claim. MPI behavior follows the MPI 3.1
collective and point-to-point contracts exposed by the current
[`mpi` 0.8.2 bindings](https://docs.rs/mpi/0.8.2/mpi/).

# Problem

The global operator is the symmetric tridiagonal matrix with diagonal `2` and
adjacent entries `-1` on twelve degrees of freedom. Balanced contiguous
ownership ensures every multi-rank run crosses partition boundaries.

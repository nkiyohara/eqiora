# Problem

The executable evidence uses the symmetric tridiagonal operator with diagonal
`2` and adjacent entries `-1` on twelve global degrees of freedom. This small
operator crosses every balanced partition boundary while retaining an obvious
global reference action.

# Problem

The evidence reuses the canonical Poisson source at
`verify/numerics/poisson-fem-fvm/models/poisson.eqi` and its default P1
finite-element realization. Only operator placement changes between the two
runs.

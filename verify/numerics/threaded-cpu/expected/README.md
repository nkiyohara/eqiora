# Acceptance

No frozen decimal table is required. The executable test requires exact
one-worker/four-worker solution and true-residual equality under the
reproducible policy, plus exact execution provenance and fail-closed worker
capability behavior.

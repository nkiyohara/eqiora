# Models

The authoritative public model fixtures remain in
`verify/fsi/fixed-reference-monolithic-step-2d/models/`. Reusing them prevents
a second copy of the same physical claim from drifting.

The Stokes and elasticity consumers likewise retain their existing registered
fixtures. This directory intentionally contains no duplicate model source.

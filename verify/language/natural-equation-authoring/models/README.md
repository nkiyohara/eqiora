# Model fixtures

`natural.eqi` authors three equalities. `explicit-residual.eqi` authors the
same ordered residuals equal to zero. Both use the current Relation header.
Their exact bytes are the respective formatter goldens; redundant hashes and
old-header byte counts are not separate authorities.

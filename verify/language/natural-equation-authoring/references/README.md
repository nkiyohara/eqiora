# Observation boundary

The executor observes the public language AST/formatter, `ModelDocument` and
its accepted Kernel program, structural comparison/fingerprint, exact package
authoring and locked compilation, native drafts, and source diagnostics.
Its small `Tree` projection reads ordered operands; fixed expected trees are
enumerated independently. It neither calls the compiler's normalization helper
as an oracle nor pins output-derived digests.

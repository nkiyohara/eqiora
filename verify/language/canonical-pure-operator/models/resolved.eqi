model Main {
  domain body = box(0, 1, 0, 1);
  representation space = continuum;
  field left on body as space: vector<1, 2>;
  field right on body as space: vector<1, 2>;
  relation balance continuous on body {
    div(div(ops.dyadic(left, right))) = 0;
  }
}

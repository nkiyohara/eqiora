connector Pin {
  across potential: 1;
  through flow: 1;
}

component Leaf(
  port p: Pin
) {
  relation owner { p.potential = 0; }
}

component Wrapper(
  port p: Pin
) {
  instance leaf: Leaf();
  connect p, leaf.p;
}

model Network() {
  instance left: Wrapper();
  instance right: Leaf();
  connect left.p, right.p;
}

# Fixtures

- `nary.eqi` declares one three-member conserving fragment.
- `chain.eqi` declares the same equivalence class as two overlapping binary
  fragments.
- `wrapper-exposure.eqi` extends an internal Wrapper net through an ownerless
  public exposure Port.
- `flat-nary.eqi` and `flat-chain.eqi` prove that the hierarchy-free parsed
  Model path uses the same pre-Kernel maximal-set normalization.
- `invalid-missing-membership.eqi` leaves one owned physical endpoint outside
  every conserving set.
- `invalid-double-owner.eqi` assigns one physical endpoint to two Relations.
- `invalid-ownerless-set.eqi` connects only ownerless public endpoints, so no
  canonical Connection can retain the required two owned members.

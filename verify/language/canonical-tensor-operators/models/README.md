# Model fixture

`elastic-relation.eqi` is the smallest source form that composes both canonical
tensor structure operators with the existing spatial differential vocabulary.
The constants have pressure dimension, so the inner sum is a stress-shaped
quantity and its divergence is a vector balance supported on `body`.

The fixture contains no boundary conditions or Realization selection and must
not be used as evidence of a solvable elasticity model.

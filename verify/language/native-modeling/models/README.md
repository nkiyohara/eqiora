# Model fixture

`decay.eqi` is the textual projection of the scalar implicit relation
`derivative(x) + rate * x = 0`. The integration test constructs the same
relation from client-neutral native declarations without reading or generating
source for that path.

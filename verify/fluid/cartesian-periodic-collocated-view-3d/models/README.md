# Model input

This case reuses the exact accepted sources from
[`cartesian-periodic-topology-3d`](../../cartesian-periodic-topology-3d/models/periodic-box.eqi)
and its
[`declaration-permuted counterpart`](../../cartesian-periodic-topology-3d/models/periodic-box-permuted.eqi).
The Rust evidence includes those existing bytes directly and compiles them
through the ordinary current Model path.

This directory intentionally contains no `.eqi` copy. It is a non-authoritative
locator only: the owning peer case retains the sole source fixtures, so this
exact-instance evidence cannot drift into a second Model authority.

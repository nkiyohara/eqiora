# Reference authority

The case is bound to immutable Git commit, tree, and blob objects; the root and
member Cargo manifest bytes and root lock bytes obtained from those objects;
and the already accepted private Cargo grammar and independent seal. Those
authorities are already bound by the accepted contract and precommitted test.

This explanatory leaf introduces no reference, expected value, digest, count,
array, tolerance, derivation, registry, compatibility promise, or general
Cargo claim. It does not duplicate sealed values or turn an external contract
or seal locator into a repository registry.

The directory contains no donor material, scratch data, machine-specific
locator, fixture, executable input, or runtime reference artifact.

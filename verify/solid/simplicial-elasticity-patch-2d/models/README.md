# Model boundary

This numerical element case consumes the existing intrinsic two-dimensional
isotropic Lamé law on a Cartesian box. It introduces no new canonical model or
source-language construct. The bounded realization data are recorded in
`case.toml`, and the authored triangle mesh is under `fixtures/`.

# Expected evidence

- Direct and exact-package authoring lower to equal bounds, density, Lamé
  coefficients, load expression, Field roles, and complete boundary roles.
- The exact package retains the four immutable `0.3.0` declarations before
  adding its two dynamic Components and resolves only through the exact
  mechanics dependency digest.
- Every accepted side traces velocity while its interface traction derives
  from displacement stress.
- Zero traction and a compatible unresolved velocity Port remain explicit
  `FluxZero` and `PortBinding` dispositions.
- Near misses fail during compilation or canonical lowering, before mesh,
  quadrature, assembly, time integration, or solve.

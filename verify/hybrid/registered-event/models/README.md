# Model source

The executable fixture is assembled from public Semantic Kernel Fields,
Parameters, Relations, Activations, and edges in
`crates/eqiora/tests/canonical_time_execution.rs`. It introduces no
bouncing-ball-specific node and no backend-side model representation.

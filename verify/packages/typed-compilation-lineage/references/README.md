# References

The canonical package, compilation, and current Model identities are derived by
the repository-owned `typed_package_compilation_lineage` test from the locked
in-memory package store. This case makes no numerical execution claim and uses
no external scientific reference.

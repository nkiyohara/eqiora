connector Pin {
  across potential: 1;
  through flow: 1;
}

component ClosedLeaf(
  port a: Pin,
  port b: Pin
) {
  relation law {
    a.potential + b.potential = 0;
  }
  connect a, b;
}

component BrokenParent() {
  instance leaf: ClosedLeaf();
  connect leaf.a, leaf.b;
}

model Main() {}

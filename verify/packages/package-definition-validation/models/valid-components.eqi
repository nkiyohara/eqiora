public component Leaf(
  parameter period: s,
  parameter offset: s = period
) {
  relation invariant { offset - period = 0; }
}

public component Wrapper(
  parameter period: s
) {
  instance leaf: Leaf(period = period);
}

connector Pin = scalar_physical(across = 1, through = 1);

component Broken(
  port terminal: conserving on Pin
) {
  relation first { across(terminal) = 0; }
  relation second { through(terminal) = 0; }
}

model Main() {}

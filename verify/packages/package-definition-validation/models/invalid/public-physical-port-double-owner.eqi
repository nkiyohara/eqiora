connector Pin {
  across potential: 1;
  through flow: 1;
}

component Broken(
  port terminal: Pin
) {
  relation first { terminal.potential = 0; }
  relation second { terminal.flow = 0; }
}

model Main() {}

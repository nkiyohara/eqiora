model Valid {}

model Invalid {
  domain line = box(0, 1);
  representation space = continuum;
  field displacement on line as space: m = 0;
  relation invalid continuous on line { normal(grad(displacement)) = 0; }
}

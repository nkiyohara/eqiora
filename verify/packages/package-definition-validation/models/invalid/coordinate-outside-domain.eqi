model Valid {}

model Invalid {
  domain line = box(0, 1);
  representation space = continuum;
  field position on line as space: m = 0;
  relation invalid continuous on line { coordinate(1) - position = 0; }
}

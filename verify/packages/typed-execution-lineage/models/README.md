# Model

The executable source is the ordinary package tree at
[`packages/org.example.poisson`](../../../../packages/org.example.poisson/).
The integration test includes those exact manifest, source, and documentation
bytes so verification cannot drift into a separate fixture model.

# Model inputs

The registered test uses the ordinary package trees under `packages/` and
prepares releases and the exact resolution at runtime. This directory contains
no checked-in current release, lock, store, or identity expectation; it remains
only because the current case runner requires a `models/` locator for every
registered case.

# References

The oracle follows RFC 0035. For every component at each interface point,
trace values agree and parent-outward flux values sum to zero. With Euclidean
pairing, these two laws imply zero aggregate interface power. All measurements
in this bounded case are exact small integers, so no tolerance or external
solver reference is required.

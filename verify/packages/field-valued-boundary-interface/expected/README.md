# Expected evidence

The Rust evidence test owns the exact structured assertions. A passing result
requires canonical identity invariance, four typed scalar junction rows,
independent zero and nonzero interface diagnostics, complete v3 replay, legacy
wire rejection, and pre-exposure failure for both geometry and support
falsifiers. No generated floating-point baseline is checked in.

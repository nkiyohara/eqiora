# Reference contract

For a Cartesian box of ambient dimension `d`, the complete exterior contains
exactly one exact Boundary Domain for every `(axis, Lower|Upper)` pair. This
identity bijection, rather than lexical side names or list positions, is the
reference oracle.

# Model fixtures

`complete.eqi` pairs one statically elaborated exterior family with four
ordinary singular terminals, yielding a verification-private exact side
bijection. `forwarded.eqi` inserts one wrapper, forwards the proved exterior
to its child, and connects the two families pointwise. The Rust conformance
root owns source/declaration/order and exact-package alias variants; invalid
fixtures live under `../invalid/`.

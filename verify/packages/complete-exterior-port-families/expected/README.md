# Expected observations

The completed case must record ordinary flattened Port, Relation, Activation,
and Connection observations only. No complete-exterior set, family binder,
member selector, forwarding alias, or package name may survive into the
Semantic Model.

Failure fixtures must demonstrate that validation finishes before any
Transaction or partial Model is exposed.

For the nested 2D fixture the exact observations are eight retained Ports,
eight Relations, eight continuous Activations, four maximal conserving
Connections, and four eliminated wrapper-Port projections. Each family member
has the same accepted Port payload and residual structure as its corresponding
ordinary singular declaration.

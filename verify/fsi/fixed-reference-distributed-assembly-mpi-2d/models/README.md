# Model fixture

This case intentionally reuses the direct fixed-reference FSI source at
[`fsi.fixed-reference-monolithic-step-2d`](../../fixed-reference-monolithic-step-2d/models/direct.eqi).
MPI changes only the execution of the already lowered assembly contract; it
does not create another Semantic Model or numerical method.

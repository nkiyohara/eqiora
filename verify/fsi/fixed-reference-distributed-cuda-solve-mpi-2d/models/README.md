# Model fixture

This case reuses the direct fixed-reference FSI source from
[`fsi.fixed-reference-monolithic-step-2d`](../../fixed-reference-monolithic-step-2d/models/direct.eqi).
MPI plus CUDA changes only execution of the accepted distributed numerical
Realization; it introduces no new Semantic Model or physical relation.

# Model fixture

This case reuses the direct fixed-reference FSI source at
[`fsi.fixed-reference-monolithic-step-2d`](../../fixed-reference-monolithic-step-2d/models/direct.eqi).
MPI changes only the accepted distributed assembly and solver execution of
the existing finalized numerical Realization. It does not introduce another
Semantic Model, FSI equation, pressure policy, discretization, or finish.

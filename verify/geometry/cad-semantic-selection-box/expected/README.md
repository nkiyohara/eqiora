# Expected observations

- The STEP stock is exactly one closed planar cuboid in millimetres.
- The constrained sketch has zero remaining degrees of freedom.
- The intersection is one closed solid, one closed shell, and six planar faces.
- The exact Model body projects to six tetrahedra and six boundary Domains
  project to twelve triangles in total.
- Viewport and table selection produce identical Geometry/Domain requests.
- The selected `x_upper` boundary exposes its canonical Relations and connected
  field-physical Ports.
- Changed source bytes, source units, CAD policy, adapter identity, accepted
  box topology, geometry revision, or selection source fail closed.
- Explicit one-to-one cross-revision association retains the selected semantic
  boundary across two independently accepted plans.
- Missing, split, merged, and ambiguous successor relations remain falsified by
  the separately registered `geometry.fixed-reference-interface-identity-2d`
  case that owns the shared association contract.
- Repair and kernel face rank are absent from the admitted public request
  vocabulary; they are type-level exclusions rather than runtime options.

The root registered target observes the canonical application projection.
Studio browser interaction, keyboard parity, responsive layout, and
accessibility are covered by local Studio tests and are not claimed as a second
root-registered evidence target.

# Reference contract

RFC 0079 is the identity authority. The independent reference route is
`expected/derive_digest.py`, which applies its written canonical ordering,
wire-field, float-format, and digest-framing rules without importing or reading
the Rust implementation.

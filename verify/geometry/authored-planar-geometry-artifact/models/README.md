# Model fixtures

There is no Semantic Model fixture in this case. The artifact is deliberately
defined before Model admission; the Rust evidence constructs only the bounded
`PlanarRegion` named by the case contract.

# Stokes dissipation profile binding (E1)

This registered structural case binds the exact noncircular selector
`a = [2e-3, 0]` to the independently precommitted reference and refined
topologies, preserves the analytic exact-area profile identity, retains the
complete Model-owned outer velocity `U e_x`, and replays every essential
vertex before Stokes system admission.

The positive path runs before the profile, topology, boundary-role,
normal-only, callback, correspondence, and fake-refinement mutants. The
evidence is owned independently by the two Rust test modules named in
[`references/README.md`](references/README.md).

This case computes no Stokes state, dissipation, gradient, history, refined
objective ordering, candidate, or publication result. It adds no public API,
durable geometry wire, optimizer, remesher, or general vector-expression
surface.

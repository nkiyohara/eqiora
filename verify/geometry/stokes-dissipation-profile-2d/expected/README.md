# Expected structural result

The ordinary positive must bind the same exact nonzero analytic profile to
both identity-distinct topologies, preserve analytic area independently of
the chordal polygon, retain exact Model and boundary-relation identities, and
replay `U e_x` on every outer essential vertex with zero body velocity.

Every registered mutant must reject at its named structural boundary after
the corresponding positive is usable. This E1 package owns no Stokes solve,
objective value, derivative, optimization history, or refinement-ordering
number.

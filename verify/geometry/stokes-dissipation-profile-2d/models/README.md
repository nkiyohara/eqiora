# Model fixture

The executable fixture is compiled into the private `eqiora-numerics` E1
evidence modules. It constructs the exact Model and Geometry association from
the precommitted structural input identified in
[`references/README.md`](../references/README.md); this directory introduces
no second Model authority or user-facing model format.

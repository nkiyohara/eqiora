# Model witness

The complete authored model is the canonical 1292-byte JSON constant embedded
in `../oracle.py` and reproduced by the ordinary Rust graph constructor. No
STEP, B-rep, mesh, or provider-local topology fixture participates.

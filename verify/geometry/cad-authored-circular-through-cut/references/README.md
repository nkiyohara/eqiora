# References

The registered case and its analytic derivation own the frozen operation and
wire contract. The executable Python oracle is standalone and uses only that
public claim; it imports no Eqiora implementation.

The existing rectangle-extrusion evidence remains the regression authority for
v1 bytes, digest, polyhedral observations, and v1 durable handles.

# References

The claim and frozen operation/wire contract are owned by GitHub Issue 271,
child slice 2 of Issue 249. The executable Python oracle is standalone and
uses only the frozen public claim; it imports no Eqiora implementation.

The existing rectangle-extrusion evidence remains the regression authority for
v1 bytes, digest, polyhedral observations, and v1 durable handles.

# Reference route

RFC 0079's standalone Python route owns the canonical geometry bytes and
digest. RFC 0080 independently specifies how those immutable facts map to
semantic support and which artifact-bundle and parent-lookup mutants must fail.

# Expected observations

The positive geometry identity and entity-set dimensions are frozen by RFC
0079's independent Python derivation. RFC 0080 precommits the bundle-closure,
parent-relative lookup, support, and diagnostic expectations exercised by the
registered Rust test.

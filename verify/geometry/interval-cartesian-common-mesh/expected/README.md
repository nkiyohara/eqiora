# Expected

Coordinates are `[-1, 0, 1, 2]`; ordered cells are `[0,1]`, `[1,2]`, and
`[2,3]`. The left endpoint is vertex/facet `0`, with parent cell `0` and local
side `0`. The right endpoint is vertex/facet `3`, with parent cell `2` and
local side `1`. Both incidence orientations are identity.

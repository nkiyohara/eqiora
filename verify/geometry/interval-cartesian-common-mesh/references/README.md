# References

The reference is the directly enumerated uniform partition of `[-1, 2]` into
three segments. Coordinates and connectivity follow from exact integer steps;
the two endpoint incidences follow from the reference segment topology. No
implementation-produced artifact supplies an expectation.

# Reference provenance

The expected values were derived independently before implementation by the two
routes recorded in `../expected/independent-oracles.md`. No production output,
fixture, or implementation source was an input to either route.

# Model inputs

No separate model file is frozen for this case. The source is the immutable
authored rectangle-extrusion graph already verified by
`geometry.cad-authored-rectangle-extrusion`; the exact witness parameters and
selected graph-bound face are recorded in `../case.toml`.

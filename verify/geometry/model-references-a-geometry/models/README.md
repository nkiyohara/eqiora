# Model fixtures

The registered Rust test constructs these fixtures through public typed graph
definitions because geometry-backed Domain syntax is an explicit nonclaim.
Each positive Model contains two distinguishable geometry regions, one
geometry boundary, one unrelated Cartesian Domain, and one global Relation.
Fresh-ID and isolated-mutation variants reuse that same bounded structure.

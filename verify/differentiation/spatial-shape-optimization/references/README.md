# References

The derivative oracle recompiles and resolves distinct canonical Domain
revisions at positive and negative bound perturbations, then forms a centered
difference. It does not call the analytic geometry action.

The square optimum is an independent symmetry reference for the fixed-area
aspect-ratio problem: exchanging axes maps `s` to `-s`, so the declared
algebraic mean objective is even and the square is stationary. The executable
test additionally requires descent from a nonsquare initial point.

# Reference

The implementation JVP is analytic. Its independent oracle recompiles the
same canonical source at symmetric Parameter perturbations, resolves and
solves each FEM/FVM realization again, and forms a centred finite difference.
No external dataset is required.

# Analytic reference

For initial height one, zero initial velocity, gravity `g`, and restitution
`e`, the first impact occurs at `t = sqrt(2/g)` with `v^- = -g t`. The test
evaluates the event-time derivative, implicit reset derivatives, saltation
matrix, and fixed-time post-event sensitivities directly from these formulas.

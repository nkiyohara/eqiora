# Model source

The executable fixture is built directly from public Semantic Kernel nodes in
`crates/eqiora-runtime/tests/canonical_hybrid_sensitivity.rs`. No
bouncing-ball-specific node or parallel model semantics is used.

# Eqiora.Electrical.Basic

The first ordinary Eqiora Model Package: one scalar electrical connector and
three ideal, time-independent components. It has no privileged lookup path,
built-in identity, execution backend, or package-specific compiler behavior.

This initial package is deliberately narrow. It does not claim nonlinear,
transient, hybrid, thermal, signal, fixed-point, or production component
coverage.

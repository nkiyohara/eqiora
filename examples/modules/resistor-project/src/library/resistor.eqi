module library.resistor;

public component Resistor {
  public parameter resistance: 1;

  relation law continuous {
    resistance - 2 = 0;
  }
}

public model ResistorCheck {
  instance load: Resistor(resistance = 2);
}
